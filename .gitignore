# =============================================================================
# DO NOT COMMIT (ignore these – local / generated / secrets)
# =============================================================================

# ----- Virtual environment (one per project – best practice) -----
.venv/
venv/
env/
ENV/
.env.local
.env.*.local

# ----- Secrets and env with keys -----
.env
# Google service account key (case law Drive backup)
auspicious-crow-432016-p8-6eb8c2e545cb.json

# ----- Python bytecode and build -----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ----- Test and tool caches -----
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.cover

# ----- IDE and OS -----
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ----- App-specific (runtime / cache) -----
.streamlit/secrets.toml
data/

# =============================================================================
# COMMIT (do not add these to .gitignore – they belong in the repo)
# =============================================================================
# .env.example   – template only, no secrets
# All .py under src/, scripts/, tests/
# requirements.txt, Makefile, README.md, docs/, setup.sh
# scripts/migrations/*.sql
# .gitignore, pyproject.toml (if added)
